
nihk 
你好

这些是 Vue.js 框架特有的“指令”。只要看到冒号 : 或艾特 @ 开头，通常就是 Vue 的语法。
    :class="...": Vue 语法。全称是 v-bind:class。
    意思是：“不要只读死也是的 class，请根据后面大括号里的逻辑，动态地决定要不要加某个 class。”

    @click="...": Vue 语法。全称是 v-on:click。
    意思是：“当用户用鼠标点击这里时，执行后面的命令。”